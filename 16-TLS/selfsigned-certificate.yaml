apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-selfsigned-cert
  namespace: default
spec:
  secretName: web-selfsigned-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
  - demo.local
