apiVersion: v1
kind: Pod
metadata:
  name: pod-fail
  namespace: dev
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    resources:
      requests:
        cpu: 3
        memory: 1Gi
      limits:
        cpu: 6
        memory: 2Gi
